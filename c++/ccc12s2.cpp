#include <bits/stdc++.h>

using namespace std;

map<char, int> mp = {{'I' : 1}, {'V', 5}, {'X', 10}, {'L', 50}, {'C', 100}, {'D', 500}, {'M', 1000}}

int main() {
	string s;
	cin >> s;
	int total = 0;
	for (int i = 0; i < s.length(); i += 2) {
		int d = s[i] - '0';
		char r = s[i + 1];
		if()
	}
}